type Vault @entity {
  id: ID!
  owner: Bytes!
  asset: Asset!
  name: String!
  symbol: String!
  decimals: Int!
  startDate: BigInt!
}

type Asset @entity {
  id: ID!
  name: String!
  symbol: String!
  decimals: Int!
}
